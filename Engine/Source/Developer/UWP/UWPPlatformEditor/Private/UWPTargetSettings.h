
/*=============================================================================
	UWPTargetSettings.h: Declares the UUWPTargetSettings class.
=============================================================================*/

#pragma once

#include "WindowsTargetSettings.h"
#include "UWPTargetSettings.generated.h"

/**
 * Implements the settings for the UWP target platform.
 */
UCLASS(config = Engine, defaultconfig)
class UUWPTargetSettings
	: public UObject
{
public:

	GENERATED_UCLASS_BODY()

	virtual void PostInitProperties() override;

	/**
	* Service Configuration GUID. Must match Dev Center configuration.
	*/
	UPROPERTY(EditAnywhere, config, Category = XboxLive)
	FString ServiceConfigId;

	/**
	* Title ID. Must match Dev Center configuration.
	*/
	UPROPERTY(EditAnywhere, config, Category = XboxLive)
	FString TitleId;

	/**
	* Whether this title's Xbox Live access is via the Xbox Live Creators Program.
	*/
	UPROPERTY(EditAnywhere, config, Category = XboxLive)
	uint32 bIsCreatorsProgramTitle : 1;

	/*
	* UWP includes SM4 support (D3D Feature level 10) by default.  This setting excludes this
	* support, possibly reducing package size, but limiting the range of devices the package
	* will run on.  Currently you must leave this DISABLED if you wish to run as UWP on Xbox One.
	*/
	UPROPERTY(EditAnywhere, config, Category = Rendering, Meta=(DisplayName="Exclude Shader Model 4 Support"))
	uint32 bExcludeShaderModel4Support : 1;

	/*
	* Selects between the D3D12 RHI and the D3D11 RHI
	*/
	UPROPERTY(EditAnywhere, config, Category = Rendering, Meta=(DisplayName = "Use D3D12 RHI"))
	uint32 bUseD3D12RHI : 1;

	/**
	* Whether cooked content should be copied into the loose package image generated by deploy from Visual Studio.
	*/
	UPROPERTY(EditAnywhere, config, Category = "Toolchain", Meta = (DisplayName = "Copy Cooked Content for F5 Deployment"))
	uint32 bCopyCookedContentForF5Deployment : 1;

	UPROPERTY(EditAnywhere, config, Category = Packaging)
	FString SigningCertificate;

	UPROPERTY(EditAnywhere, config, Category = Packaging)
	FString Logo;

	UPROPERTY(EditAnywhere, config, Category = Packaging)
	FString SmallLogo;

	UPROPERTY(EditAnywhere, config, Category = Packaging)
	FString WideLogo;

	UPROPERTY(EditAnywhere, config, Category = Packaging)
	FString SplashScreen;

	UPROPERTY(EditAnywhere, config, Category = Packaging)
	FString StoreLogo;

	UPROPERTY(EditAnywhere, Category = Packaging, meta=(HideAlphaChannel))
	FColor TileBackgroundColor = FColor::FromHex(FString("#000040"));

	UPROPERTY(EditAnywhere, Category = Packaging, meta=(HideAlphaChannel))
	FColor SplashScreenBackgroundColor = FColor::FromHex(FString("#000040"));

	UPROPERTY(EditAnywhere, config, Category = Packaging)
	FString TileBackgroundColorHex = FString::Printf(TEXT("#%02X%02X%02X"), TileBackgroundColor.R, TileBackgroundColor.G, TileBackgroundColor.B);

	UPROPERTY(EditAnywhere, config, Category = Packaging)
	FString SplashScreenBackgroundColorHex = FString::Printf(TEXT("#%02X%02X%02X"), SplashScreenBackgroundColor.R, SplashScreenBackgroundColor.G, SplashScreenBackgroundColor.B);

	/**
	* Identifies the device family that your package will target.
	*/
	UPROPERTY(EditAnywhere, config, Category = "OS Info")
	FString TargetDeviceFamily;

	/**
	* Minimum version of the universal platform required to run this title.
	* It will not be possible to deploy the build on earlier versions.
	*/
	UPROPERTY(EditAnywhere, config, Category = "OS Info", Meta = (DisplayName = "Minimum supported platform version"))
	FString MinimumPlatformVersion;

	/**
	* Specifies the maximum version of the universal platform on which the
	* title is known to work as expected.  When deployed to later versions
	* the title will experience the runtime behavior of the version given here.
	*/
	UPROPERTY(EditAnywhere, config, Category = "OS Info", Meta = (DisplayName = "Maximum tested platform version"))
	FString MaximumPlatformVersionTested;

	/** The compiler version to use for this project. May be different to the chosen IDE. */
	UPROPERTY(EditAnywhere, config, Category = "Toolchain", Meta = (DisplayName = "Compiler Version"))
	ECompilerVersion CompilerVersion;

	/**
	* List of supported <Capability><Capability> elements for the application.
	*/
	UPROPERTY(EditAnywhere, config, Category = Capabilities)
	TArray<FString> CapabilityList;

	/**
	* List of supported <Capability><DeviceCapability> elements for the application.
	*/
	UPROPERTY(EditAnywhere, config, Category = Capabilities)
	TArray<FString> DeviceCapabilityList;

	/**
	* List of supported <Capability><uap:Capability> elements for the application.
	*/
	UPROPERTY(EditAnywhere, config, Category = Capabilities)
	TArray<FString> UapCapabilityList;

	/**
	* List of supported <Capability><uap2:Capability> elements for the application.
	*/
	UPROPERTY(EditAnywhere, config, Category = Capabilities)
	TArray<FString> Uap2CapabilityList;

	/**
	* Provides outbound access to the Internet and networks in public places like airports and cofee shops.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bInternetClient;

	/**
	* Provides inbound and outbound access to the Internet and networks in public places like airports and cofee shops.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bInternetClientServer;

	/**
	* Provides inbound and outbound access to the Internet and networks that have an authenticated domain controller,
	* or that the user has designated as either home or work networks. Inbound access to critical ports is always blocked.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bPrivateNetworkClientServer;

	/**
	* Allows AllJoyn-enabled apps and devices on a network to discover and interact with each other.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bAllJoyn;

	/**
	* Allows apps to generate code dynamically.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bCodeGeneration;

	/**
	* Provides access to the microphone's audio feed, which allows the app to
	* record audio from connected microphones.
	* Required for Windows.Media.SpeechRecognition APIs.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bMicrophone;

	/**
	* Provides access to the webcam's video feed, which allows the app to capture snapshots and movies from connected webcams.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bWebCam;

	/**
	* Provides capability to connect devices in close proximity to the PC via near field proximity radio or Wi-FI Direct.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bProximity;

	/**
	* Provides access to the current location, which is obtained from dedicated hardware like a GPS sensor
	* in the PC or derived from available network information.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bLocation;

	/**
	* Allows communication with paired Bluetooth devices over the Generic Attribute (GATT)
	* or Classic Basic Rate (RFCOMM) protocols.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bBluetooth;

	/**
	* Provides capability to add, change, or delete files in the Music Library for the local PC and HomeGroup PCs.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bMusicLibrary;

	/**
	* Provides capability to add, change, or delete files in the Pictures Library for the local PC and HomeGroup PCs.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bPicturesLibrary;

	/**
	* Provides capability to add, change, or delete files in the Videos Library for the local PC and HomeGroup PCs.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bVideosLibrary;

	/**
	* Allows apps to read SMS and MMS messages that have been blocked by the Spam Filter app.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bBlockedChatMessages;

	/**
	* Allows apps to read and delete Text Messages. It also allows apps to store chat messages in
	* the system data store.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bChat;

	/**
	* Subject to Store policy. Provides ability to connect to enterprise intranet resources that require
	* domain credentials. This capability is typically not needed for most apps.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bEnterpriseAuthentication;

	/**
	* Provides access to the user's 3D Objects, allowing hte app to enumerate and access all files in the library
	* without user interaction.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bObjects3D;

	/**
	* Allows apps to access all phone lines on the device and perform the following functions:
	* place a call, access line-related metadata, access line-related triggers, set and check block
	* list and call origination information.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bPhoneCall;

	/**
	* Provides capability to add, change, or delete files on removable storage devices.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bRemovableStorage;

	/**
	* Subject to Store policy. Provides capability to access software and hardware certificates
	* for validating a user's identity.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bSharedUserCertificates;

	/**
	* Gives apps the ability to access user's name and picture.
	* Required to access Windows.System.UserProfile APIs.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bUserAccountInformation;

	/**
	* Allows access to the VOIP calling APIs in Windows.ApplicationModel.Calls.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bVoipCall;

	/**
	* Provides access to environment data, which will be used to generate spatial maps or stages.
	* Required to access Windows.Perception.Spatial APIs.
	*/
	UPROPERTY(EditAnywhere, Category = Capabilities)
	bool bSpatialPerception;
};
